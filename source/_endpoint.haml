
%h2=title
%p=subtitle
%dl
  %dt Endpoint
  %dd=action
  %dt Parameters
  - if parameters.kind_of?(Array)
    -parameters.each do |p|
      %dd
        =partial("parameter", locals: { parameter: p })
  - else
    %dd
      =parameters.to_s
  %dt Examples
  %dd=partial("dfs_dl", locals: { node: examples } )
  %dt Response
  %dd 
    - if response && !response.empty?
      =partial("response", locals: { response: response } )
    - else
      None
